// src/config/constants.ts

export const CHART_CONFIG = {
  HISTORY_LIMIT: 40,
  MAX_FORCE_KG: 15,
  MIN_MAX_FORCE: 5,
  MIN_MAX_VELOCITY: 2,
};

export const POLLING_INTERVALS = {
  REALTIME_MS: 1000,
};
